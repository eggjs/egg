const pkg = require('../../../../../node_modules/egg');

for (const key in pkg) {
  module.exports[key] = pkg[key];
}
module.exports.default = pkg;
module.exports.startCluster = pkg.startCluster;
module.exports.Application = pkg.Application;
module.exports.Agent = pkg.Agent;

setTimeout(() => {
  // console.log(process.execArgv)
  // console.log(process.env);
  console.log('exit by master test end');
  process.exit(0);
}, process.platform === 'win32' ? 10000 : 5000);
