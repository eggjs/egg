coverage:
  status:
    project:
      default:
        target: auto
        # Allow small coverage fluctuations between runs.
        threshold: 1%
    patch:
      default:
        # Give small patches a stable target independent of project coverage.
        target: 75%
        threshold: 0%
